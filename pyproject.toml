[project]
name = "smart-water-openenv"
version = "0.1.0"
description = "OpenEnv smart water environment"

[tool.openenv]
entry_point = "app:app"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
